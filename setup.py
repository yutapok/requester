from setuptools import setup, find_packages

requires = [
    "aiohttp"
    "async-timeout"
    "asyncio"
]

setup(
    name='requester',
    version='0.0.1',
    description='simple web request framework',
    author='yutapok',
    author_email='yutautsugi@outlook.com',
)

